# Setup

install python packages:

```
pip install flask flask-sqlalchemy flask-login
```

run *app.py*

in the console it should say:

```
* Running on http://127.0.0.1:5000
```

[open the adress in the browser](http://127.0.0.1:5000)